# Overrides for local preview only (./preview.sh). Keeps URLs clean at
# http://127.0.0.1:4000 while _config.yml keeps the provisional published paths.
baseurl: ""
url: "http://127.0.0.1:4000"
img: "/docs/learn/assets"
scores: "/library/learn"
markdown: kramdown
